# Copy this file to .env (or .env.production) and fill in real values for deployment.
# Not needed for local dev — the app falls back to http://localhost:5000/api.

VITE_API_BASE=https://your-backend.up.railway.app/api
